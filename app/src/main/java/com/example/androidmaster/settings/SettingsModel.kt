package com.example.androidmaster.settings

import android.os.CombinedVibration

data class SettingsModel(var volumen:Int,
                         var bluetooth: Boolean,
                         var darkMode: Boolean,
                         var vibration: Boolean)
